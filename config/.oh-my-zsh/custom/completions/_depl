#compdef depl.sh
cmds=($(~/bin/_depl list))
_arguments \
  '1:command:(${cmds})' \
  '*:commands:->remote'

if [[ $state == 'remote' && $line[1] == 'remote' ]]; then
  _arguments '2:remote:(${cmds})'
fi